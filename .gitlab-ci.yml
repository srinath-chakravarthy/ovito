image: ubuntu:latest

before_script:
  - apt-get install --yes cmake

build_linux:
   stage: build
   tags:
      - linux
   script:
      - mkdir build
      - cd build
      - cmake -DCMAKE_BUILD_TYPE=Debug ..
      - make